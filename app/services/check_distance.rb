class CheckDistance
  def initialize(track)
    # if track.trackable_type == 'Aggressor'
    # if track.trackable_type == 'Victim'


    # track é uma vitima?
    #   verificar se o agressor está perto
    #   @aggressor = track.trackable.aggressor

    # track é um agressor?
    #   verificar se a vitima está perto
    #   @victim = track.trackable.victim
     
    # pra você pegar a vítima...
    # @victim = track.trackable

    # pra você pegar o aggressor...
    # @agressor = track.trackable



    # depois de carregar as variaveis e saber  se é vitima oou agressor 

    if track.trackable_type == 'Aggressor'
  end
end